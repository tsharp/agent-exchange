{
  "name": "geist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "node scripts/build-hooks.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "validate": "node scripts/validate-hooks.mjs",
    "test": "npm run typecheck && node scripts/build-hooks.mjs --check && npm run validate && node --test scripts/test-hooks.mjs scripts/test-installed-hooks.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "^0.25.12",
    "smol-toml": "^1.8.0",
    "typescript": "^5.9.3"
  }
}
